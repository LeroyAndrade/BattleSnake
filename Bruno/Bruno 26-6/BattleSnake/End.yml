info:
  name: End
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8082/restservices/end
  body:
    type: json
    data: |-
      {
        "game": {
          "id": "51"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
