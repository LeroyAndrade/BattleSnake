info:
  name: Lokaal persistentie
  type: http
  seq: 11

http:
  method: PATCH
  url: http://localhost:8082/restservices/snake
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "color": "#00ff01",
        "head": "default",
        "tail": "default"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
