info:
  name: info
  type: http
  seq: 4

http:
  method: GET
  url: GET http://localhost:8082/restservices
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
